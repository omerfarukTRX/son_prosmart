import 'package:flutter/material.dart';

class KapiOtomasyonuPage extends StatelessWidget {
  const KapiOtomasyonuPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Kapı Otomasyonu'),
      ),
      body: const Center(
        child: Text('Kapı otomasyonu sayfası içeriği burada olacak'),
      ),
    );
  }
}
