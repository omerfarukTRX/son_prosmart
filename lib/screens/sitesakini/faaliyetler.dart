import 'package:flutter/material.dart';

class FaaliyetlerPage extends StatelessWidget {
  const FaaliyetlerPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Faaliyetler'),
      ),
      body: const Center(
        child: Text('Faaliyetler sayfası içeriği burada olacak'),
      ),
    );
  }
}
