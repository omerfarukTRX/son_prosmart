import 'package:flutter/material.dart';

class TaleplerimPage extends StatelessWidget {
  const TaleplerimPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Taleplerim'),
      ),
      body: const Center(
        child: Text('Taleplerim sayfası içeriği burada olacak'),
      ),
    );
  }
}
